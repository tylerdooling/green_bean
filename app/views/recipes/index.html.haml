%h1
  - if @meal
    = "Recipes for #{@meal.name}"
  - else
    Listing recipes

%table
  %tr
    %th Name
    %th Type
    %th Servings
    %th Total time
    %th

  - @recipes.each do |recipe|
    %tr
      %td= recipe.name
      %td= recipe.recipe_type
      %td= recipe.servings
      %td= recipe.prep_time + recipe.cook_time
      %td
        = link_to 'Show', recipe
        = link_to 'Edit', edit_recipe_path(recipe)
        = link_to 'Destroy', recipe, :confirm => 'Are you sure?', :method => :delete

%br

- if @meal
  = link_to 'Add', "/meals/#{@meal.id}/recipes/add"
- else
  = link_to 'New Recipe', new_recipe_path
