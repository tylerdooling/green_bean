- content_for :header do
  = link_to "Back", recipes_path, "data-icon" => "back", "data-role" =>"button", :class => "ui-btn-left"
  = @recipe_type.capitalize

%ul{"data-role" => "listview", "data-inset" => "true", "data-filter" => "true"}
  - @recipes.sort.each do |r|
    %li
      = link_to r.name, recipe_path(r)
